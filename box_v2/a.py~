enp_path = ['box/1/taks/api/v0.1/login', 'box/1/taks/api/v0.1/benchmark']
ori = "/benchmark"
def check(ori, enp_path):
    for path in enp_path:
        if ori in path:
            return True
    return False

print check(ori,enp_path)
